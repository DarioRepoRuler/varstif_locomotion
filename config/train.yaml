defaults:
  - _self_
  - policy: ppo

env:
  action_scale: 0.1
  control_mode: "P"
  physics_steps_per_control_step: 4
  obs_noise: 0.
  num_history: 1
  kick_vel: 0.1
  push_interval: 50

log: True
log_dir: "projects/RL4Humanoids/outputs/"

num_envs: 4096
episode_length: 100
num_learning_iterations: 500
device: 'cuda:0'
viz: True

project: RL4Humanoids
group: Bruce-P